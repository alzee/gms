<form method="post" action="{{ path('gac_delete', {'id': gac.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gac.id) }}">
    <button class="btn">{{'Delete' | trans}}</button>
</form>
