{% extends 'base.html.twig' %}

{% block title %}Doctype{% endblock %}

{% block body %}
    <h1>Doctype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doctype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ doctype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('doctype_index') }}">back to list</a>

    <a href="{{ path('doctype_edit', {'id': doctype.id}) }}">edit</a>

    {{ include('doctype/_delete_form.html.twig') }}
{% endblock %}
