{% extends 'base.html.twig' %}

{% block title %}Process index{% endblock %}

{% block body %}
    <h1>{{'Process index' | trans}}</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>{{'Id' | trans}}</th>
                <th>{{'Name' | trans}}</th>
                <th>{{'actions' | trans}}</th>
            </tr>
        </thead>
        <tbody>
        {% for process in processes %}
            <tr>
                <td>{{ process.id }}</td>
                <td>{{ process.name }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('process_show', {'id': process.id}) }}">{{'show' | trans}}</a>
                    <a class="btn btn-warning" href="{{ path('process_edit', {'id': process.id}) }}">{{'edit' | trans}}</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{'no records found' | trans}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('process_new') }}">{{'Create new' | trans}}</a>
{% endblock %}
