{% extends 'base.html.twig' %}

{% block title %}Process{% endblock %}

{% block body %}
    <h1>{{'Process' | trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{'Id' | trans}}</th>
                <td>{{ process.id }}</td>
            </tr>
            <tr>
                <th>{{'Name' | trans}}</th>
                <td>{{ process.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('process_index') }}">{{'back to list' | trans}}</a>

    <a href="{{ path('process_edit', {'id': process.id}) }}">{{'edit' | trans}}</a>

    {{ include('process/_delete_form.html.twig') }}
{% endblock %}
