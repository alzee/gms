{% extends 'base.html.twig' %}

{% block title %}Goldclass{% endblock %}

{% block body %}
    <h1>Goldclass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ goldclass.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ goldclass.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('goldclass_index') }}">back to list</a>

    <a href="{{ path('goldclass_edit', {'id': goldclass.id}) }}">edit</a>

    {{ include('goldclass/_delete_form.html.twig') }}
{% endblock %}
