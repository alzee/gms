{% extends 'base.html.twig' %}

{% block title %}Stafftype{% endblock %}

{% block body %}
    <h1>Stafftype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stafftype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stafftype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stafftype_index') }}">back to list</a>

    <a href="{{ path('stafftype_edit', {'id': stafftype.id}) }}">edit</a>

    {{ include('stafftype/_delete_form.html.twig') }}
{% endblock %}
