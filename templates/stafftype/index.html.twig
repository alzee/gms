{% extends 'base.html.twig' %}

{% block title %}Stafftype index{% endblock %}

{% block body %}
    <h1>Stafftype index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for stafftype in stafftypes %}
            <tr>
                <td>{{ stafftype.id }}</td>
                <td>{{ stafftype.name }}</td>
                <td>
                    <a href="{{ path('stafftype_show', {'id': stafftype.id}) }}">show</a>
                    <a href="{{ path('stafftype_edit', {'id': stafftype.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('stafftype_new') }}">Create new</a>
{% endblock %}
