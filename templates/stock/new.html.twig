{% extends 'base.html.twig' %}

{% block title %}New Stock{% endblock %}

{% block body %}
    <h1>{{'Create new Stock' | trans}}</h1>

    {{ include('stock/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('stock_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
