{% extends 'base.html.twig' %}

{% block title %}New Position{% endblock %}

{% block body %}
    <h1>{{'Create new Position' | trans}}</h1>

    {{ include('position/_form.html.twig') }}

    <a href="{{ path('position_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
