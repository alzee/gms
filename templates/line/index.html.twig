{% extends 'base.html.twig' %}

{% block title %}Line index{% endblock %}

{% block body %}
    <h1>{{'Line index' | trans}}</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>{{'Id' | trans}}</th>
                <th>{{'Name' | trans}}</th>
                <th>{{'actions' | trans}}</th>
            </tr>
        </thead>
        <tbody>
        {% for line in lines %}
            <tr>
                <td>{{ line.id }}</td>
                <td>{{ line.name }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('line_show', {'id': line.id}) }}">{{'show' | trans}}</a>
                    <a class="btn btn-warning" href="{{ path('line_edit', {'id': line.id}) }}">{{'edit' | trans}}</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{'no records found' | trans}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('line_new') }}">{{'Create new' | trans}}</a>
{% endblock %}
