{% extends 'base.html.twig' %}

{% block title %}Main{% endblock %}

{% block body %}
    <h1>Main</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ main.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ main.date ? main.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ main.name }}</td>
            </tr>
            <tr>
                <th>CountChild</th>
                <td>{{ main.countChild }}</td>
            </tr>
            <tr>
                <th>Sn</th>
                <td>{{ main.sn }}</td>
            </tr>
            <tr>
                <th>PerWeight</th>
                <td>{{ main.perWeight }}</td>
            </tr>
            <tr>
                <th>TotalWeight</th>
                <td>{{ main.totalWeight }}</td>
            </tr>
            <tr>
                <th>UpstreamDoc</th>
                <td>{{ main.upstreamDoc }}</td>
            </tr>
            <tr>
                <th>Length</th>
                <td>{{ main.length }}</td>
            </tr>
            <tr>
                <th>Width</th>
                <td>{{ main.width }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ main.height }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ main.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('main_index') }}">back to list</a>

    <a href="{{ path('main_edit', {'id': main.id}) }}">edit</a>

    {{ include('main/_delete_form.html.twig') }}
{% endblock %}
