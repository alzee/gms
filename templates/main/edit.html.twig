{% extends 'base.html.twig' %}

{% block title %}Edit Main{% endblock %}

{% block body %}
    <h1>{{'Edit Main' | trans}}</h1>

    {{ include('main/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('main_index') }}">{{'back to list' | trans}}</a>

    {{ include('main/_delete_form.html.twig') }}
{% endblock %}
