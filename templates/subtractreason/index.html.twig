{% extends 'base.html.twig' %}

{% block title %}Subtractreason index{% endblock %}

{% block body %}
    <h1>Subtractreason index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for subtractreason in subtractreasons %}
            <tr>
                <td>{{ subtractreason.id }}</td>
                <td>{{ subtractreason.name }}</td>
                <td>
                    <a href="{{ path('subtractreason_show', {'id': subtractreason.id}) }}">show</a>
                    <a href="{{ path('subtractreason_edit', {'id': subtractreason.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('subtractreason_new') }}">Create new</a>
{% endblock %}
