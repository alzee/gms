<form method="post" action="{{ path('subtractreason_delete', {'id': subtractreason.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subtractreason.id) }}">
    <button class="btn">Delete</button>
</form>
