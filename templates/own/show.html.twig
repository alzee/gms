{% extends 'base.html.twig' %}

{% block title %}Own{% endblock %}

{% block body %}
    <h1>Own</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ own.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ own.date ? own.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('own_index') }}">back to list</a>

    <a href="{{ path('own_edit', {'id': own.id}) }}">edit</a>

    {{ include('own/_delete_form.html.twig') }}
{% endblock %}
