<form method="post" action="{{ path('own_delete', {'id': own.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ own.id) }}">
    <button class="btn">Delete</button>
</form>
