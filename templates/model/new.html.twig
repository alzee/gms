{% extends 'base.html.twig' %}

{% block title %}New Model{% endblock %}

{% block body %}
    <h1>{{'Create new Model' | trans}}</h1>

    {{ include('model/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('model_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
