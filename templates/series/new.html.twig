{% extends 'base.html.twig' %}

{% block title %}New Series{% endblock %}

{% block body %}
    <h1>{{'Create new Series' | trans}}</h1>

    {{ include('series/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('series_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
