{% extends 'base.html.twig' %}

{% block title %}Granule{% endblock %}

{% block body %}
    <h1>{{'Granule' | trans}}</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>{{'Id' | trans}}</th>
                <td>{{ granule.id }}</td>
            </tr>
            <tr>
                <th>{{'Name' | trans}}</th>
                <td>{{ granule.name }}</td>
            </tr>
            <tr>
                <th>{{'Granule' | trans}}</th>
                <td>{{ granule.granule }}</td>
            </tr>
            <tr>
                <th>{{'note' | trans}}</th>
                <td>{{ granule.note }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('granule_index') }}">{{'back to list' | trans}}</a>

    <a class="btn btn-warning" href="{{ path('granule_edit', {'id': granule.id}) }}">{{'edit' | trans}}</a>

    {{ include('granule/_delete_form.html.twig') }}
{% endblock %}
