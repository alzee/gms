{% extends 'base.html.twig' %}

{% block title %}Edit Granule{% endblock %}

{% block body %}
    <h1>{{'Edit Granule' | trans}}</h1>

    {{ include('granule/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('granule_index') }}">{{'back to list' | trans}}</a>

    {{ include('granule/_delete_form.html.twig') }}
{% endblock %}
