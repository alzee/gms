{{ form_start(form) }}
    {{ form_row(form.doc) }}
    <table class="table table-striped table-hover d-none" id="docTable">
      <thead>
            <tr>
                <th>{{'sn' | trans}}</th>
                <th>{{'date' | trans}}</th>
                <th>{{'dueDate' | trans}}</th>
                <th>{{'countPiece' | trans}}</th>
                <th>{{'countChild' | trans}}</th>
                <th>{{'perWeight' | trans}}</th>
                <th>{{'totalWeight' | trans}}</th>
                <th>{{'note' | trans}}</th>
            </tr>
      </thead>
        <tbody>
            <tr>
                <td id="sn"></td>
                <td id="date"></td>
                <td id="dueDate"></td>
                <td id="countPiece"></td>
                <td id="countChild"></td>
                <td id="perWeight"></td>
                <td id="totalWeight"></td>
                <td id="note"></td>
            </tr>
        </tbody>
    </table>
    {{ form_row(form.artisan) }}
    {{ form_row(form.weightGold) }}
    {{ form_row(form.weightAttach) }}
    {{ form_row(form.weight) }}
    {{ form_row(form.craft) }}
    <a id="weighing" class="btn btn-danger" href="#" color="light">{{ 'weighing' | trans}}</a>
    <button class="btn btn-info" id="saveBtn" disabled>{{ button_label|default('Save')  | trans}}</button>
{{ form_end(form) }}
