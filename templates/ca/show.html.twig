{% extends 'base.html.twig' %}

{% block title %}Ca{% endblock %}

{% block body %}
    <h1>Ca</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ca.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ca_index') }}">back to list</a>

    <a href="{{ path('ca_edit', {'id': ca.id}) }}">edit</a>

    {{ include('ca/_delete_form.html.twig') }}
{% endblock %}
