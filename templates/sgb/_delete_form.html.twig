<form class="delete" method="post" action="{{ path('sgb_delete', {'id': sgb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sgb.id) }}">
    <button class="btn btn-danger">{{'Delete' | trans}}</button>
</form>
