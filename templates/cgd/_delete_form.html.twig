<form method="post" action="{{ path('cgd_delete', {'id': cgd.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cgd.id) }}">
    <button class="btn">{{'Delete' | trans}}</button>
</form>
