{{ form_start(form) }}
    <div class="form-group">
      <label for="doc" class="required">单号</label>
      <input type="text" id="doc" name="doc" required="required" class="form-control">
    </div>
    <table class="table table-striped table-hover d-none">
      <thead>
            <tr>
                <th>{{'Sn' | trans}}</th>
                <th>{{'Date' | trans}}</th>
                <th>{{'dueDate' | trans}}</th>
                <th>{{'CountPiece' | trans}}</th>
                <th>{{'CountChild' | trans}}</th>
                <th>{{'PerWeight' | trans}}</th>
                <th>{{'TotalWeight' | trans}}</th>
                <th>{{'Note' | trans}}</th>
            </tr>
      </thead>
        <tbody>
            <tr>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
        </tbody>
    </table>
    {{ form_row(form.goldclass) }}
    {{ form_row(form.position) }}
    {{ form_row(form.weight) }}
    {{ form_row(form.note) }}
    <a id="weighing" class="btn btn-danger" href="#" color="light">{{ 'weighing' | trans}}</a>
    <button class="btn btn-info disabled">{{ button_label|default('Save')  | trans}}</button>
{{ form_end(form) }}
