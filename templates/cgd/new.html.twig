{% extends 'base.html.twig' %}

{% block title %}{{ 'Cgd'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Create new'|trans }} {{ 'Cgd'|trans }}</h1>

    {{ include('cgd/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('cgd_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
