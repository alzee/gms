{% extends 'base.html.twig' %}

{% block title %}Cgd{% endblock %}

{% block body %}
    <h1>Cgd</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cgd.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ cgd.date ? cgd.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cgd_index') }}">back to list</a>

    <a href="{{ path('cgd_edit', {'id': cgd.id}) }}">edit</a>

    {{ include('cgd/_delete_form.html.twig') }}
{% endblock %}
