{% extends 'base.html.twig' %}

{% block title %}Wage{% endblock %}

{% block body %}
    <h1>{{'Wage' | trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{'Id' | trans}}</th>
                <td>{{ wage.id }}</td>
            </tr>
            <tr>
                <th>{{'Name' | trans}}</th>
                <td>{{ wage.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wage_index') }}">{{'back to list' | trans}}</a>

    <a href="{{ path('wage_edit', {'id': wage.id}) }}">{{'edit' | trans}}</a>

    {{ include('wage/_delete_form.html.twig') }}
{% endblock %}
