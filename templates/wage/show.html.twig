{% extends 'base.html.twig' %}

{% block title %}Wage{% endblock %}

{% block body %}
    <h1>Wage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wage.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wage_index') }}">back to list</a>

    <a href="{{ path('wage_edit', {'id': wage.id}) }}">edit</a>

    {{ include('wage/_delete_form.html.twig') }}
{% endblock %}
