{% extends 'base.html.twig' %}

{% block title %}Seq{% endblock %}

{% block body %}
    <h1>Seq</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seq.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ seq.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('seq_index') }}">back to list</a>

    <a href="{{ path('seq_edit', {'id': seq.id}) }}">edit</a>

    {{ include('seq/_delete_form.html.twig') }}
{% endblock %}
