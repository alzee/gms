{% extends 'base.html.twig' %}

{% block title %}Seq index{% endblock %}

{% block body %}
    <h1>{{'Seq index' | trans}}</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>{{'Id' | trans}}</th>
                <th>{{'Name' | trans}}</th>
                <th>{{'actions' | trans}}</th>
            </tr>
        </thead>
        <tbody>
        {% for seq in seqs %}
            <tr>
                <td>{{ seq.id }}</td>
                <td>{{ seq.name }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('seq_show', {'id': seq.id}) }}">{{'show' | trans}}</a>
                    <a class="btn btn-warning" href="{{ path('seq_edit', {'id': seq.id}) }}">{{'edit' | trans}}</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{'no records found' | trans}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('seq_new') }}">{{'Create new' | trans}}</a>
{% endblock %}
