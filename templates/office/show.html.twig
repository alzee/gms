{% extends 'base.html.twig' %}

{% block title %}Office{% endblock %}

{% block body %}
    <h1>{{'Office' | trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{'Id' | trans}}</th>
                <td>{{ office.id }}</td>
            </tr>
            <tr>
                <th>{{'Name' | trans}}</th>
                <td>{{ office.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('office_index') }}">{{'back to list' | trans}}</a>

    <a href="{{ path('office_edit', {'id': office.id}) }}">{{'edit' | trans}}</a>

    {{ include('office/_delete_form.html.twig') }}
{% endblock %}
