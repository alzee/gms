{% extends 'base.html.twig' %}

{% block title %}New Office{% endblock %}

{% block body %}
    <h1>{{'Create new Office' | trans}}</h1>

    {{ include('office/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('office_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
