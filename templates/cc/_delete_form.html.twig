<form method="post" action="{{ path('cc_delete', {'id': cc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cc.id) }}">
    <button class="btn">Delete</button>
</form>
