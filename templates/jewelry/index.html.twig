{% extends 'base.html.twig' %}

{% block title %}Jewelry index{% endblock %}

{% block body %}
    <h1>{{'Jewelry index' | trans}}</h1>

    <table class="table">
        <thead>
            <tr>
                <th>{{'Id' | trans}}</th>
                <th>{{'Name' | trans}}</th>
                <th>{{'actions' | trans}}</th>
            </tr>
        </thead>
        <tbody>
        {% for jewelry in jewelries %}
            <tr>
                <td>{{ jewelry.id }}</td>
                <td>{{ jewelry.name }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('jewelry_show', {'id': jewelry.id}) }}">{{'show' | trans}}</a>
                    <a class="btn btn-warning" href="{{ path('jewelry_edit', {'id': jewelry.id}) }}">{{'edit' | trans}}</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{'no records found' | trans}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('jewelry_new') }}">{{'Create new' | trans}}</a>
{% endblock %}
