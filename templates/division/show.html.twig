{% extends 'base.html.twig' %}

{% block title %}Division{% endblock %}

{% block body %}
    <h1>{{'Division' | trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{'Id' | trans}}</th>
                <td>{{ division.id }}</td>
            </tr>
            <tr>
                <th>{{'Name' | trans}}</th>
                <td>{{ division.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('division_index') }}">{{'back to list' | trans}}</a>

    <a class="btn btn-warning" href="{{ path('division_edit', {'id': division.id}) }}">{{'edit' | trans}}</a>

    {{ include('division/_delete_form.html.twig') }}
{% endblock %}
