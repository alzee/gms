{% extends 'base.html.twig' %}

{% block title %}New Craft{% endblock %}

{% block body %}
    <h1>{{ 'Create new Craft' | trans }}</h1>

    {{ include('craft/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('craft_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
