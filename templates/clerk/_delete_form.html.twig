<form class="delete" method="post" action="{{ path('clerk_delete', {'id': clerk.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clerk.id) }}">
    <button class="btn btn-danger">{{'Delete' | trans}}</button>
</form>
