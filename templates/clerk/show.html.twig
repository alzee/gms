{% extends 'base.html.twig' %}

{% block title %}Clerk{% endblock %}

{% block body %}
    <h1>Clerk</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clerk.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ clerk.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('clerk_index') }}">back to list</a>

    <a href="{{ path('clerk_edit', {'id': clerk.id}) }}">edit</a>

    {{ include('clerk/_delete_form.html.twig') }}
{% endblock %}
