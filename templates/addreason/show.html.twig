{% extends 'base.html.twig' %}

{% block title %}Addreason{% endblock %}

{% block body %}
    <h1>Addreason</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ addreason.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ addreason.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('addreason_index') }}">back to list</a>

    <a href="{{ path('addreason_edit', {'id': addreason.id}) }}">edit</a>

    {{ include('addreason/_delete_form.html.twig') }}
{% endblock %}
