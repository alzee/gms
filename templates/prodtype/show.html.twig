{% extends 'base.html.twig' %}

{% block title %}Prodtype{% endblock %}

{% block body %}
    <h1>Prodtype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prodtype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prodtype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prodtype_index') }}">back to list</a>

    <a href="{{ path('prodtype_edit', {'id': prodtype.id}) }}">edit</a>

    {{ include('prodtype/_delete_form.html.twig') }}
{% endblock %}
