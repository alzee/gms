{% extends 'base.html.twig' %}

{% block title %}Artisan{% endblock %}

{% block body %}
    <h1>{{'Artisan' | trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{'Id' | trans}}</th>
                <td>{{ artisan.id }}</td>
            </tr>
            <tr>
                <th>{{'Name' | trans}}</th>
                <td>{{ artisan.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('artisan_index') }}">{{'back to list' | trans}}</a>

    <a class="btn btn-warning" href="{{ path('artisan_edit', {'id': artisan.id}) }}">{{'edit' | trans}}</a>

    {{ include('artisan/_delete_form.html.twig') }}
{% endblock %}
