{% extends 'base.html.twig' %}

{% block title %}Op{% endblock %}

{% block body %}
    <h1>Op</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ op.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ op.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('op_index') }}">back to list</a>

    <a href="{{ path('op_edit', {'id': op.id}) }}">edit</a>

    {{ include('op/_delete_form.html.twig') }}
{% endblock %}
