{% extends 'base.html.twig' %}

{% block title %}Op{% endblock %}

{% block body %}
    <h1>{{'Op' | trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{'Id' | trans}}</th>
                <td>{{ op.id }}</td>
            </tr>
            <tr>
                <th>{{'Name' | trans}}</th>
                <td>{{ op.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('op_index') }}">{{'back to list' | trans}}</a>

    <a href="{{ path('op_edit', {'id': op.id}) }}">{{'edit' | trans}}</a>

    {{ include('op/_delete_form.html.twig') }}
{% endblock %}
