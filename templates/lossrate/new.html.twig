{% extends 'base.html.twig' %}

{% block title %}New Lossrate{% endblock %}

{% block body %}
    <h1>{{'Create new Lossrate' | trans}}</h1>

    {{ include('lossrate/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('lossrate_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
