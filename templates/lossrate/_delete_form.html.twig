<form method="post" action="{{ path('lossrate_delete', {'id': lossrate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lossrate.id) }}">
    <button class="btn btn-danger">{{'Delete' | trans}}</button>
</form>
