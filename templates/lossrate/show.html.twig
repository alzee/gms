{% extends 'base.html.twig' %}

{% block title %}Lossrate{% endblock %}

{% block body %}
    <h1>Lossrate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lossrate.id }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ lossrate.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('lossrate_index') }}">back to list</a>

    <a class="btn btn-info" href="{{ path('lossrate_edit', {'id': lossrate.id}) }}">edit</a>

    {{ include('lossrate/_delete_form.html.twig') }}
{% endblock %}
