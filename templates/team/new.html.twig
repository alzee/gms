{% extends 'base.html.twig' %}

{% block title %}New Team{% endblock %}

{% block body %}
    <h1>{{'Create new Team' | trans}}</h1>

    {{ include('team/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('team_index') }}">{{'back to list' | trans}}</a>
{% endblock %}
