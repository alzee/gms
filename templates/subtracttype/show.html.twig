{% extends 'base.html.twig' %}

{% block title %}Subtracttype{% endblock %}

{% block body %}
    <h1>Subtracttype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subtracttype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subtracttype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subtracttype_index') }}">back to list</a>

    <a href="{{ path('subtracttype_edit', {'id': subtracttype.id}) }}">edit</a>

    {{ include('subtracttype/_delete_form.html.twig') }}
{% endblock %}
