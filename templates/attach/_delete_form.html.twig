<form method="post" action="{{ path('attach_delete', {'id': attach.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attach.id) }}">
    <button class="btn">Delete</button>
</form>
