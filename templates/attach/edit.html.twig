{% extends 'base.html.twig' %}

{% block title %}Edit Attach{% endblock %}

{% block body %}
    <h1>Edit Attach</h1>

    {{ include('attach/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('attach_index') }}">back to list</a>

    {{ include('attach/_delete_form.html.twig') }}
{% endblock %}
