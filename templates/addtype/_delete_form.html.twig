<form method="post" action="{{ path('addtype_delete', {'id': addtype.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ addtype.id) }}">
    <button class="btn">Delete</button>
</form>
