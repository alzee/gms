{% extends 'base.html.twig' %}

{% block title %}Addtype{% endblock %}

{% block body %}
    <h1>Addtype</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ addtype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ addtype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('addtype_index') }}">back to list</a>

    <a href="{{ path('addtype_edit', {'id': addtype.id}) }}">edit</a>

    {{ include('addtype/_delete_form.html.twig') }}
{% endblock %}
