<form method="post" action="{{ path('center_delete', {'id': center.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ center.id) }}">
    <button class="btn">{{'Delete' | trans}}</button>
</form>
