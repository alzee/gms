{% extends 'base.html.twig' %}

{% block title %}Cotype{% endblock %}

{% block body %}
    <h1>Cotype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cotype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cotype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cotype_index') }}">back to list</a>

    <a href="{{ path('cotype_edit', {'id': cotype.id}) }}">edit</a>

    {{ include('cotype/_delete_form.html.twig') }}
{% endblock %}
