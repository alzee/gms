{% extends 'base.html.twig' %}

{% block title %}Gbs index{% endblock %}

{% block body %}
    <h1>Gbs index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Weight</th>
                <th>Note</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gb in gbs %}
            <tr>
                <td>{{ gb.id }}</td>
                <td>{{ gb.date ? gb.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ gb.weight }}</td>
                <td>{{ gb.note }}</td>
                <td>
                    <a href="{{ path('gbs_show', {'id': gb.id}) }}">show</a>
                    <a href="{{ path('gbs_edit', {'id': gb.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('gbs_new') }}">Create new</a>
{% endblock %}
